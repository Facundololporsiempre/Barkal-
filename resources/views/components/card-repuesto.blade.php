{{-- @props(["repuesto"])

<article class="mb-8 bg-white shadow-lg rounded-lg overflow-hidden">
    <img class="w-full h-72 object-cover object-center" src="{{Storage::url($repuesto->image->url)}}" alt="">

    <div class="px-6 py-3">
        <h1 class="font-bold text-xl mb-2">
            <a href="{{route("repuestos.show", $repuesto)}}">{{$repuesto->description}}</a>
        </h1>
    </div>

    <div class="text-gray-700 text-base">
        {{$repuesto->description}}
    </div>
</article> --}}